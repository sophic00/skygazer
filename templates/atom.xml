<?xml version="1.0" encoding="utf-8"?>
<feed xmlns="http://www.w3.org/2005/Atom" xml:lang="{{ lang }}">
    <title>{{ config.title }}</title>
    {%- if config.description %}
    <subtitle>{{ config.description }}</subtitle>
    {%- endif %}
    <link href="{{ feed_url | safe }}" rel="self" type="application/atom+xml"/>
    <link href="{{ config.base_url | safe }}"/>
    <generator uri="https://www.getzola.org/">Zola</generator>
    <updated>{% if last_updated is defined %}{{ last_updated }}{% else %}1970-01-01T00:00:00Z{% endif %}</updated>
    <id>{{ feed_url | safe }}</id>
    {%- for page in pages %}
        {%- if not page.extra.disabled and not page.extra.disable %}
        <entry xml:lang="{{ page.lang }}">
            <title>{{ page.title }}</title>
            <link href="{{ page.permalink | safe }}" rel="alternate" type="text/html"/>
            <id>{{ page.permalink | safe }}</id>
            <published>{{ page.date }}</published>
            <updated>{{ page.updated | default(value=page.date) }}</updated>
            {%- if page.summary %}
            <summary type="html">{{ page.summary }}</summary>
            {%- endif %}
            <content type="html" xml:base="{{ page.permalink | safe }}">{{ page.content }}</content>
        </entry>
        {%- endif %}
    {%- endfor %}
</feed>
